spring:
  application:
    name: taskflow
  datasource:
    url: jdbc:h2:mem:testDB
    username: sa
    password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
  h2:
    console:
      enable: true
      path: /h2-console